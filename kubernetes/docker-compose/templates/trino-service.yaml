apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert -c
    kompose.version: 1.34.0 (cbf2835db)
  labels:
    io.kompose.service: trino
  name: trino
spec:
  ports:
    - name: "8383"
      port: 8383
      targetPort: 8080
  selector:
    io.kompose.service: trino
